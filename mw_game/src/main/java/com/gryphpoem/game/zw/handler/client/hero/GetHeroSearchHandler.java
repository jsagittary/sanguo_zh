package com.gryphpoem.game.zw.handler.client.hero;

import com.gryphpoem.game.zw.core.exception.MwException;
import com.gryphpoem.game.zw.core.handler.ClientHandler;
import com.gryphpoem.game.zw.pb.GamePb1.GetHeroSearchRs;
import com.gryphpoem.game.zw.service.HeroService;

/**
 * 
 * @Description 获取良将寻访信息
 * @author TanDonghai
 *
 */
public class GetHeroSearchHandler extends ClientHandler {

	@Override
	public void action() throws MwException {
//		HeroService heroService = getService(HeroService.class);
//		GetHeroSearchRs resp = heroService.getHeroSearch(getRoleId());
//
//		if (null != resp) {
//			sendMsgToPlayer(GetHeroSearchRs.ext, resp);
//		}
	}

}
